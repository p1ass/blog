# ブログ基盤の改善計画

## このドキュメントについて

このブログのアーキテクチャを調査して洗い出した課題と、その解決方針をまとめています。作業は 7 つのステップに分かれていて、上から順に着手します。

新しいセッションを始めるときは、このドキュメントと [CONTEXT.md](../CONTEXT.md) を読んでください。CONTEXT.md には Post や Label といった用語の定義があります。「どう作るか」は [CLAUDE.md](../CLAUDE.md) にあります。

進捗は「作業ステップ」の各見出しにある状態欄で管理します。ステップを終えたら状態を更新してください。

調査は 2026-09-05 時点のコードに対して行いました。記事は 77 本です。

## なぜこの作業をするのか

今後入れたい機能が 3 つあります。OGP の内製化、ダークモード、TOC の生成です。この 3 つはどれも現状のコードの上にそのまま載せられません。載せようとすると、同じ処理を 2 箇所に書くか、原理的に動かないかのどちらかになります。

土台になっている問題は 5 つです。

**MDX の変換の流れが 2 つあり、片方がワークアラウンドで出来ています。** 記事本体はビルド時に変換されます。一方で一覧ページの抜粋だけは別の流れを通り、ファイルを文字列として読んで区切り文字で切り、実行時にコンパイルしてから画像パスを文字列の置き換えで直しています。この流れには remark と rehype のプラグインが適用されていません。TOC の見出し抽出を入れるなら、2 つの流れの両方に入れる必要があります。

**どの記事かを何で見分けるかが決まっていません。** `id`、`permalink`、`fullFilePath` が並立していて、前後記事の探索はタイトル一致で行われます。カテゴリとタグは同じ構造をしているのに別々に実装されていて、片方にだけバグが残っています。この曖昧さがある限り、記事一覧まわりに機能を足すたびに実装が 2 箇所に増えます。

**色がビルド時に埋め込まれる静的な定数です。** 実行時に差し替える余地がないため、この形のままではダークモードが入りません。名前も見た目を指しているので、ダークモードでは `white` が黒を意味することになります。

**ビルドが外部サービス 2 つとリンク先サイト 196 件に依存しています。** 記事を 1 文字も変えていなくても、どれか 1 つが落ちればデプロイが失敗します。

**テストが 1 つもありません。** 検証手段は `pnpm build` が通るかどうかだけです。この後に挙げるバグ (一覧の抜粋の欠け、OG 画像の 404、無効な CSS、前後記事のタイトル引き) がどれも本番に残っているのは、ビルドを通してしまうからです。

作業の順番は、この 5 つを下から順に潰す形にしています。機能の実装 (ステップ 7) は最後です。

## 設計方針

ここに書いた内容は決定済みです。実装中に前提が崩れた場合は、このドキュメントを更新してから進めてください。

### 記事を見分けるキー

どの記事かを見分けるキーを `Slug` (記事ディレクトリの名前) に一本化します。`id`、`permalink`、`fullFilePath` はすべて Slug から導出します。

現状は 3 つの値を glob のキーから個別に文字列操作で作っていて、どれが記事を見分けるキーなのかコードから読み取れません。前後記事の探索がタイトル一致になっているのも、この曖昧さから来ています。

### カテゴリとタグ

カテゴリとタグを `Label` として共通化します。種類は `LabelKind` (`category` または `tag`) で区別します。

全件を持つ `Label` と、1 ページ分だけを取り出した `LabelPage` は別の型にします。現状は両方とも `posts` という同じ名前のフィールドを持ち、中身が全件なのか 1 ページ分なのか型から判別できません。

ルートは `LabelIndexPage` と `LabelPostsPage` の 2 コンポーネントに集約し、`categories/` と `tags/` 配下の 6 ファイルはそれを呼ぶだけにします。

記事からラベルへの関係の持ち方 (カテゴリは必ず 1 つ、タグは 0 個以上) を共通化しません。ここはカテゴリとタグで性質が違います。

### frontmatter の検証

zod でスキーマを定義し、記事の集約時に全件を検証します。検証に失敗したらビルドを落とします。

あわせて `categories: string[]` を `category: string` に変えます。型は配列ですが、実際に使われているのは先頭の 1 要素だけで、全 77 記事が 1 つしか持っていません。

日付は `+09:00` 付きの ISO 8601 に統一します。現状 75 本がオフセットなし、2 本がオフセット付きで揺れています。統一後は `parseDate` の `Asia/Tokyo` 固定という補正が不要になります。

### 抜粋の生成

recma プラグインを書いて、ビルド時に `ContentSummary` を MDX モジュールから export します。

これにより `MarkdownRenderer.tsx` の実行時コンパイル、`fs.readFileSync` による記事の読み直し、`split('---')` での切り出し、`replaceAll('./')` による画像パスの書き換え、`node:fs` への依存がまとめて消えます。記事本体と同じ remark/rehype 構成が抜粋にも適用されます。

### テスト

vitest を導入します。`import.meta.glob` で記事を集める層と、`Post[]` を受け取る純粋関数の層を分け、後者にテストを書きます。層を分けること自体が設計の改善になります。

### 見出し階層

1 ページにつき h1 を 1 つに揃えます。ヘッダーのサイト名はトップページでのみ h1 とし、それ以外のページでは div にします。見た目は変わりません。

記事本文の `##` は h2 になります。記事タイトルを h1 にすれば階層が自然に成立し、TOC を h2 と h3 で組めます。

### 色システム

色を役割で呼ぶ名前 (`text` / `textMuted` / `surface` / `border` / `accent` など) に改名し、値は `var(--color-*)` を返すようにします。

CSS 側は 3 段構成にします。`:root` に既定値を置き、`@media (prefers-color-scheme: dark)` 内の `:root:not([data-theme='light'])` で OS 設定に追従させ、`:root[data-theme='dark']` で明示的な選択を優先させます。

### ダークモード

3 状態のトグル (`system` から `light`、`dark`、`system` の順に巡回) を置きます。選択は localStorage に保存します。

`app/islands/ThemeToggle.tsx` を作ります。ちらつきを防ぐため、head に同期実行のインラインスクリプトを 1 つ置いて、CSS より前に `data-theme` を確定させます。

### TOC

記事本文は 800px 中央寄せのまま動かさず、**右**の余白に sticky で目次を置きます。余白が足りない画面幅では、記事の冒頭に折りたたみで出します。

対象は h2 と h3 です。h2 が 3 個未満の記事 (7 本) では目次を出しません。現在位置のハイライトは island と IntersectionObserver で実装します。

### OGP の内製化

リンクカードの OGP は自前で取得し、結果を `ogp-cache.json` としてリポジトリにコミットします。ビルド時はキャッシュにない URL だけ取得し、取得に失敗したらドメイン名だけの素のリンクにフォールバックします。キャッシュの更新は `pnpm ogp:refresh` の手動実行とし、自動更新の仕組みは作りません。

OG 画像は Satori と resvg でビルド時に生成します。フォントはリポジトリに同梱し、日本語の改行制御は budoux で入れます。frontmatter で `ogImage` を指定している 9 記事はその指定を優先し、残り 68 記事は生成画像を使います。

既存の外部サービスは [p1ass/og-image](https://github.com/p1ass/og-image) (vercel/og-image のフォーク) にあります。1200 × 630、下辺に 16px の `#4172B5` のボーダー、タイトルは Noto Sans JP の 600 で色は `#1E2126`、フッターにサイト名と URL とアイコン、という構成です。リンクカード側の `blog-api` は `github.com/otiai10/opengraph` を使った Go のサービスで、レスポンスの型は `app/lib/ogp.ts` にそのまま写っています。

### 外部スクリプト

FontAwesome の kit をやめ、インライン SVG に置き換えます。実際に使っているのは Author の GitHub と Twitter のアイコン、Note の info アイコンだけです。

Twitter の `widgets.js` は head から削除し、`Twitter` コンポーネントを island にしてマウント時に動的に読み込みます。埋め込みがあるのは 77 記事中 16 記事だけです。

Pocket のシェアボタンは削除します。Pocket は 2025 年 7 月にサービスを終了していて、リンクが死んでいます。置き換えは行わず、X とはてなブックマークだけ残します。

## 課題一覧

対応するステップの番号を右端に書いています。

### 記事データモデル

`app/lib/posts.ts` は glob のキーから `id`、`permalink`、`fullFilePath` の 3 つを個別の文字列操作で作ります。どれが記事を見分けるキーなのかコードから読み取れず、`PostPagination` は受け取った `id` にさらに `.replace(/\/index\.mdx$/, '')` をかけ直しています。

前後記事の探索はタイトルで `findIndex` しているため、同名の記事を書いた時点で誤った結果を返します。さらに見つからなかった場合 `findIndex` は -1 を返し、その結果として一覧の先頭の記事が「前の記事」として黙って表示されます。エラーにならないので気づけません。

カテゴリとタグは型と処理のどちらも同じ形をしているのに、別々に実装されています。その結果 `getTagPosts` にだけ「`tag.posts` を持っているのに `getAllPosts().filter()` をやり直す」という無駄が残りました。片方を直しても、もう片方には反映されません。

frontmatter は型の宣言だけで検証がありません。実際に日付フォーマットが 2 本で揺れていて、`categories` は型が配列なのに全 77 記事が 1 つしか持っていません。

日付の扱いにはもう 1 つ問題があります。`parseDate` は `parse(str, 'YYYY-MM-DDTHH:mm:ss', 'Asia/Tokyo')` と書かれていますが、この第 3 引数が無視されており、返る Date はビルドマシンの TZ に依存します。同じ frontmatter から、ローカル (JST) と CI (UTC) で 9 時間ずれた瞬間が得られます。`YYYY/MM/DD` の表示はパースと整形が同じズレで相殺されるため無事ですが、機械可読な日時を出す箇所では表面化します。

| ID | 内容 | 場所 |  |
| --- | --- | --- | --- |
| A-1 | 抜粋が本文中の `---` で切れる。`filter-data-structure` で発生中 | `app/components/PostSummarySection.tsx:95-96` | 4 |
| A-2 | `ContentSummary` を export するプラグインが存在せず、型だけが残っている | `app/lib/posts.ts:11,37,64` | 4 |
| A-3 | 前後記事の探索がタイトル一致。同名記事で誤った結果を返す | `app/lib/posts.ts:92-102` | 1 |
| A-4 | slug から URL を導出する処理が 3 箇所に散在 | `app/lib/posts.ts:57-61`, `app/components/PostPagination.tsx:38,47` | 3 |
| A-5 | `getTagPosts` が `tag.posts` を使わず filter をやり直す。カテゴリ側と非対称 | `app/lib/posts.ts:220` | 3 |
| A-6 | ID 生成が `toLowerCase()` のみ。日本語やスペースを含む名前で URL が不正になり、衝突検出もない | `app/lib/posts.ts:158,200` | 3 |
| A-7 | `categories` の型が配列だが、実際は単数として使っている | `app/routes/posts/types.ts:5` | 2 |
| A-8 | frontmatter が実行時に検証されない。日付フォーマットが 2 本で揺れている | `app/lib/time.ts:4` | 2 |
| A-9 | RSS と sitemap だけ `parseDate` を通さず生の文字列を渡している | `app/routes/index.xml.ts:37`, `app/routes/sitemap.xml.ts:23` | 2 |
| A-10 | `parseDate` の `Asia/Tokyo` 指定が無視され、返る Date がビルドマシンの TZ に依存する | `app/lib/time.ts:4` | 2 |

### MDX の二重の変換

一覧ページの抜粋は、記事本体とはまったく別の流れで描画されます。`PostSummarySection` が mdx ファイルを `fs.readFileSync` で文字列として読み、`{/* <!--more--> */}` で切り、`split('---')[2]` で frontmatter を落とします。それを `MarkdownRenderer` が実行時に MDX としてコンパイルします。

この流れには remark と rehype のプラグインが一切適用されていません。プラグインを入れるとエラーになるためコメントアウトされています。そのため一覧ページでは表とシンタックスハイライトと Mermaid が適用されません。画像パスはコンパイル済みの JavaScript 文字列に対する `replaceAll('./', imgBaseUrl)` という見境のない置き換えで解決しています。本文に `./` が現れると誤って書き換えます。

`split('---')[2]` は本文中の水平線でも切れます。`filter-data-structure` の抜粋が冒頭の挨拶までしか出ていないのは、これが原因です。

TOC はこの流れの上に乗ります。見出しの抽出とアンカーの付与を両方の流れに入れる必要があり、しかも片方はプラグインを受け付けない状態です。

| ID | 内容 | 場所 |  |
| --- | --- | --- | --- |
| B-1 | 抜粋側に remark/rehype が適用されず、一覧で表とハイライトと Mermaid が適用されない | `app/components/MarkdownRenderer.tsx:13-22` | 4 |
| B-2 | 画像パスをコンパイル済み JS への見境のない置き換えで解決している | `app/components/MarkdownRenderer.tsx:28` | 4 |
| B-3 | 一覧ページごとに実行時 MDX コンパイルが 10 回動く | `app/components/PostSummarySection.tsx` | 4 |
| B-4 | 変換の流れが 2 つあるため、TOC 用の見出し抽出を両方に入れる必要がある | — | 4 |

### スタイリング

色は `app/styles/color.ts` の静的な定数で、hono/css がビルド時にリテラル値を CSS へ埋め込みます。実行時に差し替える余地がないため、この形のままではダークモードが入りません。

名前も障害になります。`gray`、`white`、`backgroundDark` はどれも見た目を指しているので、ダークモードでは `white` が黒を意味することになります。名前が嘘をつく状態で色を足していくと、後から読む人が値を確認しないと何色か分からなくなります。

さらに、定数を使うという規約自体が守られていません。`_renderer.tsx` にはハードコードした `#dde0e4` があり、`ShareIcons.tsx` には 6 色が直接書かれています。`PostSummarySection.tsx` の `border-top: 1px solid $border;` は Sass 記法の残骸で、CSS として無効なため border が表示されていません。

| ID | 内容 | 場所 |  |
| --- | --- | --- | --- |
| C-1 | 色が静的な定数で、ビルド時にリテラルが埋め込まれる | `app/styles/color.ts` | 5 |
| C-2 | 色名が役割ではなく見た目を指す。ダークモードで `white` が黒になる | `app/styles/color.ts` | 5 |
| C-3 | ハードコードした色が残っている | `app/routes/_renderer.tsx:40` | 1 |
| C-4 | highlight.js のテーマ 60 行超が直書き。ライトとダークの 2 テーマが要る | `app/routes/_renderer.tsx:61-140` | 5 |
| C-5 | `transition.ts` があるのにベンダープレフィックスのコピペが 5 箇所 | 5 ファイル | 5 |
| C-6 | Sass 記法の残骸で CSS として無効。border が出ていない | `app/components/PostSummarySection.tsx:31` | 1 |
| C-7 | ハードコードした色が 6 つ | `app/components/ShareIcons.tsx` | 5 |

### HTML とメタ情報

本番で不具合を抱えたまま放置されているものが混ざっています。

フォールバックの OG 画像は、参照が `/static/ogp.png`、実体が `public/static/opg.png` です。綴りが食い違ったまま 404 を返しています。frontmatter を持たないページ (トップ、カテゴリ、タグ、404) が SNS で共有されると、すべてこの画像が使われます。

Pocket のシェアボタンは、 Pocket が 2025 年 7 月にサービスを終了した後もそのまま全記事に出ています。1 記事につき上下 2 箇所です。

見出し階層も破綻しています。ヘッダーのサイト名が h2 で、その後に h1 が来ます。一覧ページでは記事タイトルごとに h1 が並びます。TOC を h2 と h3 で組むには、先にここを整える必要があります。

| ID | 内容 | 場所 |  |
| --- | --- | --- | --- |
| D-1 | RSS の link に `title='TODO'` が残っている | `app/routes/_renderer.tsx:212` | 1 |
| D-2 | `og:type` が記事ページでも `website` 固定。`article:published_time` もない | `app/routes/_renderer.tsx:177` | 1 |
| D-3 | canonical がない | `app/routes/_renderer.tsx` | 1 |
| D-4 | 見出し階層が破綻。一覧で h1 が並び、ヘッダーのサイト名が h2 | `app/components/Header.tsx`, `app/components/PostSummarySection.tsx:105` | 4 |
| D-5 | `Use-Agent:` の綴りが誤り。正しくは `User-agent:` | `app/routes/robots.txt.ts:4` | 1 |
| D-6 | `base` が本番の絶対 URL 固定で、プレビューデプロイでも本番を指す | `vite.config.ts:23-24` | 見送り |
| D-7 | フォールバックの OG 画像が 404。参照は `ogp.png`、実体は `opg.png` | `app/routes/_renderer.tsx:165` | 1 |
| D-8 | Pocket のシェアボタンが死んでいる | `app/components/ShareIcons.tsx:136` | 1 |
| D-9 | シェア URL のスキームが `http://` | `app/components/ShareIcons.tsx` | 1 |
| D-10 | シェア URL のクエリを未エンコードで組み立てている。`#` を含むタイトルの 2 記事で共有文が途中で切れる | `app/components/ShareIcons.tsx` | 1 |

### 外部依存とビルド

ビルドが 3 種類の外部要因に依存しています。`blog-api.p1ass.com` (リンクカードの OGP 取得)、`og-image.p1ass.com` (OG 画像の生成)、そしてリンク先サイト 196 件そのものです。キャッシュはビルド内のモジュールスコープにしかなく、リトライとフォールバックのどちらもありません。どれか 1 つが落ちれば、記事を 1 文字も変えていない場合でもデプロイが失敗します。

テストは 1 つもありません。`app/lib/posts.ts` はほぼ純粋関数の塊で、ページネーションとタグの集約と slug の変換は、どれも本来テストしやすい部類です。ここまでに挙げたバグがどれも本番に残っているのはビルドを通してしまい、他に検証手段がないからです。

| ID | 内容 | 場所 |  |
| --- | --- | --- | --- |
| E-1 | `blog-api.p1ass.com` の取得に失敗すると、ビルドは成功し、不完全なページが出力される。55 記事 196 箇所を毎回取得 | `app/lib/ogp.ts:50` | 6 |
| E-2 | `ogp-parser` が依存に入っているが使われていない | `package.json:20` | 6 |
| E-3 | OG 画像が `og-image.p1ass.com` への外部依存。URL が 2 箇所にベタ書き | `app/routes/_renderer.tsx:162`, `app/routes/index.xml.ts:32` | 6 |
| E-4 | FontAwesome と Twitter widgets を全ページで読み込んでいる | `app/routes/_renderer.tsx:191-200` | 6 |
| E-5 | テストが 1 つもない | — | 2 |
| E-6 | `viteCommonjs` の 12 パッケージと debug のパッチに理由の記録がない | `vite.config.ts:29-44` | 6 |

## 作業ステップ

### ステップ 1: 実害のあるバグ

状態: 完了

対象は A-3、C-3、C-6、D-1、D-2、D-3、D-5、D-7、D-8、D-9、D-10 です。D-10 は着手後に見つけました。

目的は本番で現に不具合が出ているものを止めることです。OG 画像の 404、Pocket のデッドリンク、表示されない border、前後記事の誤リンクはどれも今この瞬間に読者から見えています。構造には手を入れないので、以降のステップと衝突しません。

互いに独立した小さな修正の集まりなので、まとめて 1 つの PR にします。ここでは構造に手を入れません。

完了条件は次のとおりです。

- `public/static/opg.png` を `ogp.png` に改名し、参照と一致させる
- Pocket のシェアボタンを削除し、シェア URL を `https://` にする
- シェア URL のクエリを `encodeURIComponent` に通す
- `og:type` を記事ページで `article` にし、`article:published_time` を出す
- 全ページに canonical を出す
- 前後記事の探索をファイルパス引きに変え、見つからない場合はリンクを出さない
- `pnpm build` が通り、`pnpm lint` が通る

A-1 (抜粋が `---` で切れる) はここに含めません。ステップ 4 で構造的に解消されるため、今応急処置を入れると捨てるコードを書くことになります。実害は 1 記事だけです。

着手時に判明した点を 2 つ残します。1 つはシェア URL のクエリが未エンコードだったことです (D-10)。タイトルに `#` を含む記事が 2 本あり、共有時のテキストがそこで切れていました。もう 1 つは前後記事の探索で、タイトルが一致しなかった場合に `findIndex` が -1 を返し、一覧の先頭が「前の記事」として表示される状態だったことです。今回のファイルパス引きへの変更とあわせて、見つからない場合はリンクを出さないようにしました。

さらに、追加した `article:published_time` の値がローカルと CI で 9 時間ずれることから A-10 が見つかりました。暫定の対処として `toIso8601Jst` を `app/lib/time.ts` へ追加し、Date を経由せず文字列としてオフセットを付けています。`parseDate` そのものはステップ 2 で置き換えます。

### ステップ 2: テスト基盤と frontmatter の検証

状態: 完了

対象は A-7、A-8、A-9、A-10、E-5 です。

目的は 2 つあります。1 つはステップ 3 と 4 を安全に進めることです。この 2 つは既存の挙動を保ったまま構造を組み替えるため、テストが先にないと挙動が変わった事実に気づけません。もう 1 つは frontmatter の揺れを二度と入れないことです。日付フォーマットが 2 本で揺れているのも、`categories` が配列のままなのも、書いた時点で誰も止めなかったからです。

完了条件は次のとおりです。

- vitest が動き、`pnpm test` で実行できる
- `app/lib/posts.ts` を、記事を集める層と純粋関数の層に分ける
- ページネーションの境界値にテストがある
- zod のスキーマで frontmatter を検証し、失敗時にビルドが落ちる
- `categories: string[]` を `category: string` に変え、全 77 記事を追従させる
- 全 77 記事の日付を `+09:00` 付きの ISO 8601 に統一する
- `parseDate` を、TZ から独立した実装に置き換える (A-10)
- ステップ 1 で入れた `toIso8601Jst` を、統一後の frontmatter に合わせて整理する
- RSS と sitemap が `parseDate` を経由する
- 日付の整形をビルドマシンの TZ から独立させる
- CI が `pnpm test` を実行する

着手して分かったことを残します。

**frontmatter に 3 件の不備が埋まっていました。** スキーマ検証を入れた瞬間にビルドが落ちて見つかったものです。`intuition-in-programming` は `tags:` が空で YAML の null になっていました。`node-env` は `description` が空で、RSS に空の説明が出ていました。`pocket-saved-entries-2021` には Hugo 時代の名残の `toc: true` が残っていて、どのコードも読んでいませんでした。TOC をステップ 7 で作るときに、記事ごとの出し分けが要るかどうかを改めて決めます。

**日付の整形も TZ に依存していました。** A-10 は `parseDate` の問題として記録していましたが、`format` の側にも同じ問題がありました。`parseDate` を正しくすると、JST の深夜に投稿した記事が UTC のビルドマシンでは前日として表示されます。整形にも `tz` を明示する `formatDate` を用意し、`vitest` は `TZ=UTC` で回してこれを検証しています。

**RSS の `pubDate` が不正でした。** 12 時間表記の `hh` を使っていたため、13 時以降に投稿した記事の時刻がずれていました。オフセットも `+00:00` と RFC 822 非準拠でした。`2025-07-14T19:00:00` の記事が `Mon, 14 Jul 2025 07:00:00 +00:00` として配信されていた状態です。A-9 の修正にあわせて直しました。

**CI の biome が固定されていませんでした。** ワークフローが `biomejs/setup-biome` の `version: latest` を使っていたため、ローカルの 2.3.14 に対して CI では 2.5.12 が動き、新しく追加されたルールでこの PR が落ちました。コードを変えていなくても biome のリリースだけで CI が赤くなる状態だったので、`package.json` に固定した版を使うように変えました。

**CI の textlint が全記事を対象にしてしまう問題が出ました。** frontmatter の移行が 77 記事すべてに触れるため、既存の文章の指摘 1042 件で CI が赤くなります。ワークフローの意図は「既存記事の指摘で CI が赤くならないようにする」ことなので、対象を「本文が変わったファイル」に絞る `scripts/list-prose-changes.mjs` を追加しました。

### ステップ 3: Slug と Label のモデル整理

状態: 完了

対象は A-4、A-5、A-6 です。

目的は記事を見分けるキーを 1 つに決め、URL をそこから導出する形にすることです。カテゴリとタグの実装が 1 つになるため、以降の変更が片方だけに入る事故がなくなります。ステップ 7 で目次やテーマを足すとき、触る場所が 1 箇所で済むかどうかがここで決まります。

完了条件は次のとおりです。

- `Slug` を導入し、`permalink` と `fullFilePath` をそこから導出する
- 文字列操作による URL 導出が `app/lib/posts.ts` の 1 箇所だけになる
- `Label`、`LabelKind`、`LabelPage` を導入する
- `LabelId` の生成が日本語とスペースを含む名前を扱え、衝突をビルド時に検出する
- `categories/` と `tags/` 配下の 6 ファイルが、共通コンポーネントを呼ぶだけになる
- 生成される URL がステップ 2 以前と一致する (`dist` を比較して確認する)

URL が変わると既存の被リンクが切れるため、最後の項目は必ず確認してください。

着手して分かったことを残します。

**空白を含むタグが 9 件ありました。** `Claude Code` や `VS Code` などで、URL が `/tags/claude code/` という形になっていました。`href` に生の空白が入った不正な URL です。A-6 を直すとこの 9 件の URL が変わるため、完了条件と衝突します。タグページは sitemap に載せておらず、外部からの被リンクもほぼ無いと判断して、ハイフンに変えました。`/tags/claude-code/` になります。

日本語のタグとカテゴリ、空白を含まないタグの URL は 1 つも変わりません。`dist` を比較したところ、424 ファイル中の差分はこの 9 件だけでした。

**表記ゆれを静かに潰していました。** `Go` と `go` の両方を書いた場合、以前は同じ id に潰れて片方の記事が一覧から抜け落ちていました。`buildLabels` で衝突を検出し、ビルドを落とすようにしました。

**ルートの 6 ファイルが 286 行から 26 行になりました。** `LabelIndexPage` と `LabelPostsPage`、そして `ssgParams` を含む `label-routes.tsx` に集約しています。

### ステップ 4: MDX の変換の一本化と見出し階層

状態: 完了

対象は A-1、A-2、B-1、B-2、B-3、B-4、D-4 です。

目的は MDX の変換の流れを 1 つにすることです。抜粋のバグが構造ごと消え、一覧ページでも記事本体と同じ描画になり、TOC の実装先が 1 箇所になります。

このステップが今回の中心です。ここを飛ばして TOC に進むと、見出しの抽出を 2 つの流れへ書くことになり、しかも片方はプラグインを受け付けません。

完了条件は次のとおりです。

- recma プラグインが `ContentSummary` を export する
- `app/components/MarkdownRenderer.tsx` を削除する
- `PostSummarySection` から `node:fs` と `node:path` への依存がなくなる
- 一覧ページの抜粋で表とシンタックスハイライトが適用される
- `filter-data-structure` の抜粋が `---` で切れない
- 1 ページにつき h1 が 1 つになる
- ビルド時間を計測し、着手前と比較する
- ステップ 2 の時点の出力と本文を比較し、意図しない変化が無いことを確認する

判断の経緯は [docs/adr/0001-抜粋をビルド時に-export-する.md](./adr/0001-抜粋をビルド時に-export-する.md) にあります。

着手して分かったことを残します。

**ステップ 3 で見出しを壊していました。** `/categories/` の見出しが「Categorys」になっていました。接頭辞に `s` を足す実装にしたためです。ステップ 3 では `dist` のファイル一覧しか比べておらず、本文の変化を見ていませんでした。今回はステップ 2 の時点でビルドした結果と本文まで比較し、意図した 9 ページ以外に差が無いことを確認しています。

**E-1 の影響が想定より大きいことが分かりました。** 計画には「外部サービスが落ちるとビルドが落ちる」と書いていました。しかし実際にはビルドが落ちません。`fetchOgp` が投げると SSG がそれを握りつぶし、`Internal Server Error` という中身の `index.txt` を書き出します。今回のビルド中に実際に発生し、`/posts/java-catch-up/` が不完全なページとして出力されました。次のビルドでは成功したため、断続的な失敗です。ステップ 6 でフォールバックを入れるときは失敗を握りつぶさないことも条件に加えます。

**ビルド時間は 40 秒から 34 秒になりました。** キャッシュを消した状態での計測です。実行時の MDX コンパイルを無くした分だけ速くなりました。

### ステップ 5: 色システムのセマンティック化

状態: 完了

対象は C-1、C-2、C-4、C-5、C-7 です。

目的は実行時に色を差し替えられる形へ変えることです。ダークモードの前提であり、これがない限りステップ 7 へ進めません。

完了条件は次のとおりです。

- `app/styles/color.ts` が役割ベースの名前になり、値が `var(--color-*)` を返す
- CSS 変数の定義が `app/styles/theme.ts` に集まる (`prefers-color-scheme` と `data-theme` の段はステップ 7 で足す)
- highlight.js のテーマがライトとダークの 2 つに分かれ、`_renderer.tsx` から切り出される
- ハードコードした色がコードベースから消える
- `transition.ts` が使われ、ベンダープレフィックスのコピペが消える
- 見た目がライトモードで着手前と変わらない

この時点ではダークモードの切り替え UI も、ダーク側の値も入れません。値を差し替えられる状態にするところまでです。OS をダークにしている読者に中途半端なダークモードが見えないよう、`prefers-color-scheme` の段はステップ 7 まで足しません。

着手して分かったことを残します。

**他社のブランド色が混ざっていました。** X の黒、はてなの青、GitHub と Twitter のアイコン色です。これらはテーマで差し替えるものではないので、`app/styles/brand.ts` に分けました。ダークモードでボタンの見え方を調整する必要が出たら、ここを見ることになります。

**見た目が変わっていないことを、生成された CSS で確認しました。** hono/css のクラス名は内容のハッシュなので、単純な文字列比較ができません。ステップ 4 の時点でビルドした結果を用意し、CSS 変数を実値に展開したうえでルール単位に突き合わせました。220 ページすべてで一致しています。

吸収した差は 2 つだけです。1 つは `body` に背景色を明示したこと (これまでは未指定でブラウザ既定の白)。もう 1 つは共通化した `transition()` が `-o-transition` を出すことです。どちらも見た目に影響しません。

### ステップ 6: OGP の内製化と外部スクリプトの整理

状態: 未着手

対象は E-1、E-2、E-3、E-4、E-6 です。

目的はビルドを自分の管理下に置くことです。今は外部サービス 2 つとリンク先サイト 196 件のどれかが落ちるだけでデプロイが失敗します。記事を書いた本人にはどうにもできない理由で公開が止まる状態をなくします。

完了条件は次のとおりです。

- OGP の取得を自前で行い、`blog-api.p1ass.com` への依存が消える
- `ogp-cache.json` がリポジトリにあり、ビルド時は未取得の URL だけ取得する
- 取得に失敗してもビルドが落ちず、素のリンクにフォールバックする
- 失敗を握りつぶさない。今は SSG が例外を飲み込み、`Internal Server Error` を中身とする `index.txt` を書き出してしまう
- `pnpm ogp:refresh` でキャッシュを更新できる
- Satori と resvg で OG 画像を生成し、`og-image.p1ass.com` への依存が消える
- `ogImage` を指定している 9 記事はその指定が優先される
- FontAwesome の kit を削除し、インライン SVG に置き換える
- `Twitter` を island にし、head から `widgets.js` を削除する
- `viteCommonjs` の列挙と debug のパッチを棚卸しし、残すものに理由を書く

終わったら `docs/adr/0002-ogp-キャッシュをコミットする.md` を書きます。

### ステップ 7: ダークモードと TOC

状態: 未着手

目的は機能を足すことです。ここまでの 6 ステップはすべてこの 2 つを無理なく載せるための土台でした。

完了条件は次のとおりです。

- `app/islands/ThemeToggle.tsx` があり、3 状態を巡回する
- 選択が localStorage に保存され、再訪時に復元される
- head の同期スクリプトによりリロード時のちらつきがない
- ダークモードでコードブロックと画像とリンクカードが破綻しない
- 記事ページの右側に sticky の目次が出る
- 記事本文の位置が一覧ページと変わらない
- 狭い画面では冒頭の折りたたみになる
- h2 が 3 個未満の記事では目次が出ない
- スクロールに応じて現在位置がハイライトされる

## 見送った選択肢

判断を蒸し返さないために、検討して選ばなかった案を残します。

**抜粋を remark の AST 分割で作る**: 実行時コンパイルのコストと画像パスのワークアラウンドが残るため見送りました。

**抜粋をやめて `description` を出す**: 最も単純ですが、一覧から画像とリンクカードが消えて見た目が変わるため見送りました。

**カテゴリとタグを別の型のまま、関数だけ共有する**: ルート 6 ファイルの重複が残るため見送りました。将来カテゴリにだけ説明文や表示順を持たせたくなった場合は、この案を再検討してください。

**OG 画像を Playwright の chromium で生成する**: rehype-mermaid が既に chromium を入れているため追加依存はゼロですが、77 枚のスクリーンショットでビルドが伸びるため見送りました。

**OGP キャッシュを定期実行で更新する**: PR が増えるため見送りました。手動スクリプトで足ります。

**OGP キャッシュに TTL を持たせる**: ビルドのたびに生成物の差分が出て、ローカルと CI でキャッシュがずれるため見送りました。

**ダークモードを OS 設定への追従だけにする**: JS がゼロで済みますが、読者が OS と違う配色を選べないため見送りました。

**TOC を記事冒頭の折りたたみだけにする**: レイアウト変更と island のどちらも不要ですが、長い記事での使い勝手を優先しました。

**目次を左側に置く**: 横書きで本文を読む際の視線を妨げるため、右側にしました。

**Twitter の読み込みを frontmatter のフラグで判定する**: 書き忘れると埋め込みが表示されなくなり、スキーマ検証では防げないため見送りました。

## 今回のスコープ外

**D-6 (`base` が本番の絶対 URL 固定)**: プレビューデプロイでも本番 URL を指しますが、実害が確認できていないため触りません。プレビュー環境を本格的に使い始めたら再検討してください。

**`_404.tsx` の内容**: `Not Foundだよ` とだけ表示します。直す価値はありますが、今回の課題からは独立しています。

**`dist` が 144MB あること**: 画像の総量によるもので、構造の問題ではありません。

**記事本文そのものの修正**: 日付フォーマットの統一と `categories` から `category` への変更以外、記事には手を入れません。
