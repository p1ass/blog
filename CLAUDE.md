# CLAUDE.md

[blog.p1ass.com](https://blog.p1ass.com) で配信しているブログのソースコードと記事を管理しているリポジトリ。

## 技術スタック

- Web フレームワーク: HonoX
- 記事: MDX
- デプロイ先: Cloudflare Pages
- スタイリング: [DESIGN.md](DESIGN.md) を参照

## コーディングエージェントのレスポンスと文章の長さ

応答は要点に絞って簡潔にする。ただし、ビジネスの場面で問題のない、正しい日本語表現を使う。注意書きや但し書きは短くし、分量の大半を本題に関する内容とする。説明を求められた際は、詳細な説明を明示的に頼まれない限り概要にとどめる。

出力する文書はタスクが必要とする長さに抑える。必要な内容は漏れなく記載し、中身のないセクションや要約の繰り返し、定型句などで無駄に長くしてはならない。

## 記事

記事は `app/routes/posts/<slug>/index.mdx` に配置し、画像も同じディレクトリに配置する。Frontmatter のスキーマは `app/routes/posts/types.ts` の Zod スキーマで定義されている。

本文には一覧ページに表示する抜粋（冒頭）の末尾を示す `{/* <!--more--> */}` マーカーを必ず挿入する。

公開前の記事は Frontmatter に `draft: true` を付ける。記事一覧や RSS から除かれ、ビルドでもページが出力されない。ただし記事ディレクトリの画像は `dist/` にコピーされるため、ドラフトには画像を置かない。

記事一覧の取得や加工処理は `app/lib/posts.ts` で実装している。また、MDX 内で使うコンポーネントや組み込みタグの差し替えを行いたい場合は `app/lib/mdx-components.tsx` に登録する。

記事のサンプルコードを実行するための API キー（`OPENAI_API_KEY`、`ANTHROPIC_API_KEY` など）は `.gitignore` に登録済みの `.env` に書かれている。

画像のパスは開発時 (`/app/routes/posts/...`) とビルド後 (`/posts/...`) で `import.meta.env.PROD` により分岐する。画像を扱うコードを変更する際は、両方の分岐で整合性を保つ。

## ビルド

- 初回は `pnpm install:playwright` を実行する。ビルド時に Chromium で Mermaid を描画するため、インストールされていないとビルドが失敗する。
- `scripts/` 配下のスクリプトは TypeScript で作成し、`node scripts/<name>.ts` で直接実行する。
- ルーティング処理内で失敗しうるもの（OGP の取得など）は、例外をスローせずフォールバックさせる。`@hono/vite-ssg` は例外が発生するとページの代わりに "Internal Server Error" を出力してビルドを正常終了扱いにしてしまうため、該当記事のみが本番環境で 404 になってしまう。
- リンクカードの OGP 情報は `ogp-cache.json` を参照する。キャッシュの更新時は `pnpm ogp:refresh` を手動で実行する。

## スタイリング

hono/css の `css` を利用し、値は `app/styles/` のデザイントークンを参照する。手順は `design-system` Skill を参照すること。

### hono/css の制約

過去に発生したハマりどころ（注意すべき既知の問題）を以下にまとめる。

- **`text-decoration` のショートハンドを書かない。** 継承したスタイルの `text-decoration-color` が初期値に戻ってしまうため、`text-decoration-line` を使う。
- **`:-hono-global` の中に複数行のコメントを書かない。** スタイルブロックが展開されず、内部のスタイルが適用されなくなる。
- **補間する値に二重引用符を含めない。** `\"` にエスケープされ、値が変わるか宣言自体が無効になる。なお、テンプレートリテラルに直接記述した文字列はそのままレンダリングされる。

## クライアントサイドレンダリング

HonoX では `app/islands/` に配置したコンポーネントがブラウザで動作する。ただし、hono/css のクラスを持つ island より後ろにレンダリングされた island は、SSR 時に `<honox-island>` でラップされずブラウザで動作しない。ヘッダーの `ThemePicker` がその制約に該当するため、他にブラウザで動かしたい処理がある場合は `app/client.ts` から呼び出す。

## ハーネス

| ハーネス             | 目的                                                                                      | 実行タイミング        |
| -------------------- | ----------------------------------------------------------------------------------------- | --------------------- |
| リグレッションテスト | ページの見た目が意図せず変わっていないことを確認する                                      | `pnpm vrt`、CI        |
| スタイルガイド       | デザイントークンとコンポーネントを 1 ページですべて確認できること                         | `/styleguide`         |
| ビルド結果のチェック | すべての記事が出力されていること、OG 画像が存在すること、本文に未許可の要素が含まれていないこと | `pnpm build` の最後   |
| スタイルチェック     | CSS がデザイントークンを参照していることを検証する                                        | `pnpm lint:style`、CI |
| コントラストの検証   | 両テーマで WCAG 2.2 AA を満たすことを検証する                                             | `pnpm test`、CI       |

### リグレッションテスト

Docker コンテナ内でビルドからスクリーンショット撮影までを行う。テストが失敗した場合は、意図しない見た目の変更が発生していることを意味する。

- **撮影が終わるまでホスト側の `pnpm build` は実行しない。** コンテナと `dist/` を共有しているため、途中で再生成すると出力が揃っていない記事が 404 になり、該当記事だけ基準画像の更新がスキップされてしまう。
- **Mermaid の図はビルドのたびに描画位置がわずかにずれることがある。** 差分が図の内部のみであれば、スクリーンショットを撮り直す。

### ビルド結果のチェック

記事本文に新しい要素を表示させる場合は、あらかじめスタイルを適用した上で `scripts/check-build-output.ts` の `allowedElements` に追加する。

## コーディング規約

コミット前に `pnpm lint:fix` を実行する。

textlint は記事だけでなく、すべての Markdown とソースコードの日本語コメントに適用される。和文と欧文の間には半角スペースを入れる。`textlint/plugins/` は `file:` 参照されており pnpm によってコピーされるため、修正した場合は `pnpm install` を実行するまで反映されない。

### コメント

コードのコメントは原則として書かない。コードから「なぜそうしたのか（WHY / 背景や意図）」が読み取れない場合に限り、短く簡潔な文で書く。経緯や過去の実装の記録は Git の履歴に委ねる。

日本語の文の途中で改行しない。1 行に複数の文が入ってもよい。

```ts
// 悪い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしない
// ようにする。

// 良い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしないようにする。
```

### AI らしい表現の検知

[textlint-rule-preset-ai-words-ja](https://github.com/p1ass/textlint-rule-preset-ai-words-ja) で検出する。記事ごとに外したい語がある場合は、`.textlintrc.json` の `no-ai-words` に `allows` を設定する。
