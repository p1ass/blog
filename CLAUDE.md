# CLAUDE.md

[blog.p1ass.com](https://blog.p1ass.com) で配信しているブログのソースコードと記事を管理しているリポジトリ。

## 技術スタック

- Webフレームワーク: HonoX
- 記事: MDX
- デプロイ先: Cloudflare Pages
- スタイリング: [DESIGN.md](DESIGN.md)を参照

## コーディングエージェントのレスポンスと文章の長さ

応答は要点に絞って簡潔にする。ただし、ビジネスの場面で出しても問題ない正しい日本語表現を使う。注意書きや但し書きは短くし、分量の大半を本題に関する内容とする。説明を求められたら、詳しい説明を明示的に頼まれない限り概要を答える。

出力する文書はタスクが必要とする長さで生成する。必要なことはすべて記載し、中身のないセクションや要約の繰り返し、決まり文句で分量を長くしてはいけない。

## 記事

記事は `app/routes/posts/<slug>/index.mdx` に配置し、画像も同じディレクトリに配置する。Frontmatter は `app/routes/posts/types.ts` の zod スキーマで定義されている。

本文には一覧ページに載せる冒頭の終わりを示す `{/* <!--more--> */}` マーカーを必ず挿入する。

記事の一覧から導出する処理は `app/lib/posts.ts` で実装している。また、MDX から使うコンポーネントや組み込みタグの差し替えを行いたい場合は `app/lib/mdx-components.tsx` に登録する。

画像のパスは開発時 (`/app/routes/posts/...`) とビルド後 (`/posts/...`) で `import.meta.env.PROD` により分岐する。画像を扱うコードを触るときは両方の分岐を揃える。

## ビルド

- 初回は `pnpm install:playwright` を実行する。Mermaid をビルド時に chromium で描画するため、無いとビルドが失敗する。
- `scripts/` の実行ファイルは TypeScript で作成し、`node scripts/<name>.ts` で直接実行する。
- ルートの処理で失敗しうるもの (OGP の取得など) は、例外を投げずにフォールバックする。`@hono/vite-ssg` は例外が出るとページの代わりに "Internal Server Error" を書き出してビルドを成功扱いにしてしまい、その記事だけが本番で 404 になってしまう。
- リンクカードの OGP は `ogp-cache.json` を参照する。更新は `pnpm ogp:refresh` を手動で実行する。

## スタイリング

hono/css の `css` を利用し、値は `app/styles/` のトークンを参照する。手順は `design-system` Skill を参照。

### hono/css の制約

これまでに発生した分かりづらい問題をまとめる。

- **`text-decoration` のショートハンドを書かない。** 取り込んだスタイルの `text-decoration-color` が初期値に戻ってしまう。`text-decoration-line` を使う。
- **`:-hono-global` の中に複数行のコメントを書かない。** ブロックが展開されず、中のスタイルが適用されなくなってしまう。
- **補間する値に二重引用符を入れない。** `\"` にエスケープされ、値が変わるか宣言ごと無効になってしまう。テンプレートに直接書いた文字列はそのままレンダリングされる。

## クライアントサイドレンダリング

HonoX では `app/islands/` に置いたコンポーネントがブラウザで動作する。ただし、hono/css のクラスを持つ island より後ろに描画した island は、SSR で `<honox-island>` にラップされずブラウザで動かない。ヘッダーの `ThemePicker` がそれにあたるので、ほかにブラウザで動かしたい処理は `app/client.ts` から呼び出す。

## ハーネス

| ハーネス             | 目的                                                                                | いつ回るか            |
| -------------------- | ----------------------------------------------------------------------------------------- | --------------------- |
| リグレッションテスト | ページの見た目が意図せず変わっていないこと                                                | `pnpm vrt`、CI        |
| スタイルガイド       | トークンとコンポーネントが 1 ページで全て把握できること                                   | `/styleguide`         |
| ビルド結果のチェック | 記事が全部出力されていること、OG 画像に実体があること、本文に知らない要素が出ていないこと | `pnpm build` の最後   |
| スタイルチェック     | CSS がトークンを参照していること                                                            | `pnpm lint:style`、CI |
| コントラストの検証   | 両テーマで WCAG 2.2 AA を満たすこと                                                       | `pnpm test`、CI       |

### リグレッションテスト

Docker の中でビルドからスクリーンショット撮影までを行う。テストが落ちたら、見た目が変わったことを意味する。

- **撮影が終わるまでホストの `pnpm build` は待つ。** コンテナと `dist/` を共有しているので、途中で作り直すと出力が揃っていない記事が 404 になり、基準画像の更新がその記事だけ飛ぶ。
- **Mermaid の図はビルドのたびにわずかに動くことがある。** 差分が図の中だけなら撮り直す。

### ビルド結果のチェック

記事本文に新しい要素を出すときは、先にスタイルを当ててから `scripts/check-build-output.ts` の `allowedElements` に追加する。

## コーディング規約

コミット前に `pnpm lint:fix` を実行する。

textlint は記事だけでなく、すべての Markdown とソースコードの日本語コメントに適用される。和文と欧文の間には半角スペースを入れる。`textlint/plugins/` は `file:` で参照していて pnpm がコピーするため、修正したら `pnpm install` を実行まで反映されない。

### コメント

コードのコメントは原則書かない。コードから WHY (背景や意図) が読み取れないときだけ、短く簡潔な文で書く。経緯や以前の実装は git の履歴に任せる。

日本語の文の途中で改行しない。1 行に複数の文が入ってもよい。

```ts
// 悪い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしない
// ようにする。

// 良い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしないようにする。
```

### AI っぽい語の辞書

[textlint-rule-preset-ai-words-ja](https://github.com/p1ass/textlint-rule-preset-ai-words-ja) で検出する。記事ごとに外したい語は `.textlintrc.json` の `no-ai-words` に `allows` を書く。
