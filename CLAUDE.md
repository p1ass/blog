# CLAUDE.md

[blog.p1ass.com](https://blog.p1ass.com) のソースコード。HonoX + MDX で書いた記事を SSG し、Cloudflare Pages へ配信する。見た目の方針は [DESIGN.md](DESIGN.md) にある。

## 応答と文書の長さ

応答は要点に絞って簡潔にする。注意書きや但し書きは短くし、分量の大半を本題に使う。説明を求められたら、詳しい説明を明示的に頼まれない限り概要を答える。

書き出す文書は、タスクが必要とする長さで書く。必要なことはすべて書き、中身のない節や要約の繰り返し、決まり文句で長くしない。

## 記事

記事は `app/routes/posts/<slug>/index.mdx` に配置し、画像も同じディレクトリに配置する。Frontmatter は `app/routes/posts/types.ts` の zod スキーマで全件を検証し、外れた記事があるとビルドが落ちる。本文には一覧に載せる冒頭の終わりを示す `{/* <!--more--> */}` を必ず入れる。

記事の一覧から導出する処理 (ページネーション、Label、前後の記事) は `app/lib/posts.ts` に書かれている。MDX から使うコンポーネントと、組み込みタグの差し替えは `app/lib/mdx-components.tsx` に登録する。

画像のパスは開発時 (`/app/routes/posts/...`) とビルド後 (`/posts/...`) で `import.meta.env.PROD` により分岐する。画像を扱うコードを触るときは両方の分岐を揃える。

## ビルド

- 初回は `pnpm install:playwright` を実行する。Mermaid をビルド時に chromium で描画するため、無いとビルドが落ちる。
- `scripts/` の実行ファイルは TypeScript で書き、`node scripts/<name>.ts` で直接動かす。
- ルートの処理で失敗しうるもの (OGP の取得など) は、例外を投げずにフォールバックする。`@hono/vite-ssg` は例外が出るとページの代わりに "Internal Server Error" を書き出してビルドを成功させ、その記事だけが本番で 404 になる。
- `pnpm build` が生成する `public/posts/<slug>/og.png` はコミットする。
- リンクカードの OGP は `ogp-cache.json` を参照する。更新は `pnpm ogp:refresh` の手動実行。

## スタイリング

hono/css の `css` で書き、値は `app/styles/` のトークンを参照する。手順は `design-system` Skill にある。

### hono/css の制約

エラーに遭遇すると見た目が崩れるうえ、原因が読み取りにくい。

- **`text-decoration` のショートハンドを書かない。** 取り込んだスタイルの `text-decoration-color` が初期値に戻る。`text-decoration-line` を使う。
- **カンマで並べたセレクタに、クラスを `${...}` で差し込まない。** `{` の直前以外に置いたクラスは、クラス名ではなく中身の宣言に展開される。中の要素は素のクラス名で指し、クラスは `cx()` で足す。
- **`:-hono-global` の中に複数行のコメントを書かない。** ブロックが展開されず、中の規則が当たらなくなる。
- **`:-hono-global` を持つクラスを `cx()` に通さない。** グローバルの規則が入れ子として出て、`body` や `:root` に当たらなくなる。本文の要素には当たり続けるので、一見それらしく描画される。`<body>` に印を付けたいときは属性を使う。
- **補間する値に二重引用符を入れない。** `\"` にエスケープされ、値が変わるか宣言ごと無効になる。テンプレートに直接書いた文字列はそのまま出る。

## ブラウザで動かす処理

HonoX では `app/islands/` に置いたコンポーネントがブラウザで動く。ただし、hono/css のクラスを持つ island より後ろに描画した island は、SSR で `<honox-island>` に包まれずブラウザで動かない。ヘッダーの `ThemePicker` がそれにあたるので、ほかにブラウザで動かしたい処理は `app/client.ts` から呼ぶ。

## ハーネス

| ハーネス             | 何を守るか                                                                                | いつ回るか            |
| -------------------- | ----------------------------------------------------------------------------------------- | --------------------- |
| リグレッションテスト | ページの見た目が意図せず変わっていないこと                                                | `pnpm vrt`、CI        |
| スタイルガイド       | トークンとコンポーネントが 1 ページで全て把握できること                                   | `/styleguide`         |
| ビルド結果のチェック | 記事が全部出力されていること、OG 画像に実体があること、本文に知らない要素が出ていないこと | `pnpm build` の最後   |
| 生の値のチェック     | CSS がトークンを通していること                                                            | `pnpm lint:style`、CI |
| コントラストの検証   | 両テーマで WCAG 2.2 AA を満たすこと                                                       | `pnpm test`、CI       |

### リグレッションテスト

Docker の中でビルドから撮影までを行い、しきい値と再試行はすべて 0 にしてある。落ちたら、それは見た目が変わったことを意味する。

- **撮影が終わるまでホストの `pnpm build` は待つ。** コンテナと `dist/` を共有しているので、途中で作り直すと出力が揃っていない記事が 404 になり、基準画像の更新がその記事だけ飛ぶ。
- **Mermaid の図はビルドのたびにわずかに動くことがある。** 差分が図の中だけなら撮り直す。

### ビルド結果のチェック

記事本文に新しい要素を出すときは、先にスタイルを当ててから `scripts/check-build-output.ts` の `allowedElements` に足す。順序を逆にすると、このチェックが「見た目を揃える」という役目を失う。

## コーディング規約

コミット前に `pnpm lint:fix` を実行する。

textlint は記事だけでなく、すべての Markdown とソースコードの日本語コメントにかかる。和文と欧文の間には半角スペースを入れる。`textlint/plugins/` は `file:` で参照していて pnpm がコピーするため、直したら `pnpm install` を回すまで反映されない。

### コメント

コードのコメントは原則書かない。コードから WHY (背景や意図) が読み取れないときだけ、短く簡潔な文で書く。経緯や以前の実装は git の履歴に任せる。

日本語の文の途中で改行しない。1 行に複数の文が入ってもよい。

```ts
// 悪い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしない
// ようにする。

// 良い例
// {platform} を入れて、macOS で撮った画像が Linux の基準画像を上書きしないようにする。
```

### AI っぽい語の辞書

[textlint-rule-preset-ai-words-ja](https://github.com/p1ass/textlint-rule-preset-ai-words-ja) で検出する。記事ごとに外したい語は `.textlintrc.json` の `no-ai-words` に `allows` を書く。
